# Environment Configuration for Dashboard Frontend
# Copy this file to .env and modify the values as needed

# Backend API URL (optional - defaults to same-origin /api)
VITE_API_URL=http://localhost:3000/api

# WebSocket URL (optional - defaults to same-origin ws://)
VITE_WS_URL=ws://localhost:3000/ws

# Node Environment
NODE_ENV=development